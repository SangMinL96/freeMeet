import CategoryModalIndex from '@screen/Modal/CategoryModalIndex';
import React from 'react';
import {Text, View} from 'react-native';
import styled from 'styled-components/native';

function CategoryModalStack() {
  return <CategoryModalIndex />;
}

export default React.memo(CategoryModalStack);
